using UnityEngine;

public class Tests {

    public static void test1_pack_unpack() {
        /*
        Vector3 pos = new Vector3(5, 6, 7);
        Vector4 rot_input = new Vector4(1, 2, 3, 4);
        rot_input.Normalize();
        Quaternion rot = new Quaternion(rot_input.x, rot_input.y, rot_input.z, rot_input.w);

        NetMsg msg_create = new SC_EntityCreated(555, 42.46f, pos, rot, 420);
        byte[] packedMsg = MessagesHandler.NetMsgPack(msg_create);
        NetMsg unpackedMsg = MessagesHandler.NetMsgUnpack(packedMsg);

        if (msg_create.Equals(unpackedMsg)) {

            Debug.Log("testing pack/unpack result: SUCCESS");
        }
        else {
            Debug.Log("testing pack/unpack result: FAILED");
            Debug.Log("original message: ID:" + msg_create.EntityID +
                " Type: " + ((SC_EntityCreated)msg_create).Type +
                " TimeStamp: " + ((SC_EntityCreated)msg_create).TimeStamp +
                " Position: " + ((SC_EntityCreated)msg_create).Position +
                " Rotation: " + ((SC_EntityCreated)msg_create).Rotation);

            Debug.Log("unpacked message: ID:" + unpackedMsg.EntityID +
                " Type: " + ((SC_EntityCreated)unpackedMsg).Type +
                " TimeStamp: " + ((SC_EntityCreated)unpackedMsg).TimeStamp +
                " Position: " + ((SC_EntityCreated)unpackedMsg).Position +
                " Rotation: " + ((SC_EntityCreated)unpackedMsg).Rotation);



        }
        */
        

    }
}